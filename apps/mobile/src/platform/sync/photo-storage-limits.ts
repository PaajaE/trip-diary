/**
 * Storage limits for photos and videos in the `photos` bucket.
 */
export const PHOTOS_BUCKET_FILE_SIZE_LIMIT_BYTES = 8_388_608

export {
  VIDEO_BUCKET_FILE_SIZE_LIMIT_BYTES,
  VIDEO_MAX_CANONICAL_BYTES,
  VIDEO_MAX_DURATION_SECONDS,
} from '@trip-diary/utils'
