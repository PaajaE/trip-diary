import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'cz.tripdiary.app',
  appName: 'Trip Diary',
  webDir: 'dist',
  backgroundColor: '#f7f4ed',
}

export default config
